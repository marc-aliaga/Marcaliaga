// Public URL of the deployed site, no trailing slash (e.g. "https://marcaliaga.com").
// Can also be set at build time with the SITE_URL env var, which takes precedence.
// Until this is set, the build skips canonical URLs and the sitemap rather than
// publishing wrong ones.
export const site = {
  url: "https://marcaliaga.com",
  name: "Marc Aliaga",
  description:
    "Personal page collecting Marc Aliaga's equity research and articles.",
};
